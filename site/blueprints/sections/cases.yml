type: pages
headline: Cases
parent: site.find("cases")
# size: tiny
# info: "{{ page.case.toDate('d.m.Y') }}"
template: case
empty: No case yet
# image:
#   # query: page.cover
#   cover: true
#   ratio: 5/4

# headline: Cases
type: pagetable
limit: 10
# sortBy: title asc
# # info: "{{ page.prenom }}"
# limit: 25
columns:
  title:
    label: Title
    text: '{{ page.title }}'
  themes:
    label: Themes
    text: '{{page.themes2}}'